/**
 * @kapable/auth — Authentication helpers for Kapable apps.
 *
 * Re-exports from @signaldb-live/connect-auth for backward compatibility.
 *
 * Usage:
 *   import { getUser, requireRole } from '@kapable/auth/server';
 *   import { useAuth } from '@kapable/auth/react';
 */

export type { ConnectUser } from '@signaldb-live/connect-auth';
